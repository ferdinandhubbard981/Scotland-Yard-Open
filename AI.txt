definitions:
PlayerList: 6 bits each representing one player
            only one of them can be one at a time

Player:
    tickets: 5 Integers representing ticket count of {TAXI, BUS, UNDERGROUND, DOUBLE, SECRET}


inputs
2d vector board state:
    each element is one hot encoded PlayerList

6 Player

numOfRoundsSinceReveal: continous integer

output: real : [-1, 1] representing value of the state from mrX' perspective
if mrX is winning the value will be closer to 1
if mrX is losing the value will be closer to -1
